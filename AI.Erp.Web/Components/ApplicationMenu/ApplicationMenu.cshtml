@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, AI.Erp.Web
@addTagHelper *, WebVella.Controls
@using AI.Erp.Web.Models;
@{
	var applicationMenu = (List<MenuItem>)ViewBag.ApplicationMenu;

}
<div class="app-sitemap">
@foreach (var menuItem in applicationMenu)
{
	@*@Html.Partial("NavMenu", menuItem)*@
	<partial name="NavMenu" for="@menuItem"/>
}
</div>